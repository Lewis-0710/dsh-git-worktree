# Peer dependencies are provided by the dsh host tree at runtime; the local
# install must not try to fetch them (host packages declare workspace:^ peers
# that are resolvable only inside the host monorepo).
auto-install-peers=false
@deepseek-ai:registry=https://registry.npmmirror.com
